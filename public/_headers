# Cloudflare Pages / Workers 静态资源响应头
# 实体数据分片是 gzip 字节流，交给前端用 DecompressionStream 解压，
# 这里显式声明类型，避免被平台按二进制流二次压缩。
#
# ⚠️ 踩坑提醒：一个请求若同时命中多条规则，同名 header 的值会被【逗号拼接】而不是覆盖。
# 所以不要写 /* 这种宽泛的 Cache-Control 规则——它会和 /entity/data/* 的值拼在一起，
# 变成 "max-age=86400, max-age=0, ..." 这种自相矛盾的头。要加规则就用互不重叠的具体路径。

/entity/data/*
  Content-Type: application/gzip
  Cache-Control: public, max-age=86400

/entity/catalog.json
  Cache-Control: public, max-age=3600

/preview/app.js
  Cache-Control: public, max-age=600

/sitemap.xml
  Cache-Control: public, max-age=3600

# 带内容哈希的构建产物（内容变了文件名才会变）→ 可永久缓存，回访不再发验证请求
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# 封面 / 路线图 / hero 背景：文件名不带哈希。
#
# ⚠️ 2026-09-26 从 7 天（604800）改成 1 分钟 + stale-while-revalidate，原因有两个：
#   1. 投稿换封面走的是**同名覆盖**（`custom/<地图英文名>.webp`），7 天缓存意味着
#      站长审核通过、站点也重建了，回访用户却要看一星期的旧封面/黑封面；
#   2. 更糟的是**404 也会被按这条规则缓存**：任何一次瞬时取不到图（部署间隙、
#      文件还没同步到边缘），用户那边就是"破图"卡 7 天，硬刷新才恢复。
# 代价很小：`stale-while-revalidate` 让回访先吃缓存、后台再校验，页面不会变慢；
# 只是每个文件每分钟最多多一次 304 条件请求。
#
# ⚠️ 注意不能单独给 `/images/covers/custom/*` 写一条更短的规则 —— 它和 `/images/*`
#    重叠，两个 max-age 会被逗号拼起来（见文件顶部那条踩坑提醒）。要改就改这一条。
/images/*
  Cache-Control: public, max-age=60, stale-while-revalidate=86400
